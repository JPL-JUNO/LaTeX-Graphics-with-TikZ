\documentclass[border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning}
\begin{document}
\begin{tikzpicture}
    \node (tex)[fill=orange, text=white]{TEX};

    \node (pdf) [fill={rgb:red,244;green,15;blue,2}, text=white, right=of tex]{PDF};

    % \draw (tex) edge[->] (pdf);
    % \draw (tex) edge[->] node[above, font=\tiny\ttfamily] {pdflatex} (pdf);

    \draw (tex) edge["pdflatex" {font=\tiny\ttfamily, above}, ->] (pdf);
\end{tikzpicture}
\end{document}